import { createTheme } from "@material-ui/core";

const theme = createTheme({
  palette: {
    primary: {
      main: "#66718F",
    },
    secondary: {
      main: "#f50057",
    },
  },
});

export default createTheme;
